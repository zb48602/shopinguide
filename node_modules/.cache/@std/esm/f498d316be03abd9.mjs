"main";let webpack;_be0‍.w('webpack',[["default",function(v){webpack=v}]]);let config;_be0‍.w('../webpack.config.prod',[["default",function(v){config=v}]]);

// const move = require('./move')

webpack(config(), (err, stats) => {
  // move()
  console.log(
    stats.toString({
      colors: true,
      modules: false,
      children: false,
      chunks: false,
      chunkModules: false
    }),
    '打包完成'
  )
})
