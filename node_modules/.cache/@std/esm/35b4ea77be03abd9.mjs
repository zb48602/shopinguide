"main";let webpack;_be0‍.w('webpack',[["default",function(v){webpack=v}]]);let config;_be0‍.w('../webpack.config.dev',[["default",function(v){config=v}]]);

// import move from './move'

const compiler = webpack(config())

compiler.watch({}, (err, stats) => {

  // move()
  console.log(stats.toString({
    colors: true,
    modules: false,
    children: false,
    chunks: false,
    chunkModules: false
  }))

})
